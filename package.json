{
  "name": "case4",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",

    "eslint-plugin-security": "^1.7.1",
    "eslint-plugin-security-node": "^3.0.0",
    "eslint-plugin-regexp": "^2.5.0",
    "eslint-plugin-redos": "^1.3.0"
  },
  "scripts": {
    "lint": "eslint . --ext .js --format json -o eslint-report.json",
    "lint:ci": "eslint . --ext .js --format json -o eslint-report.json || true"
  }
}

